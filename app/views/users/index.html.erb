<% provide(:title, 'All users') %>

<h1>All users</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= link_to user.name, user %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'View Pages', pages_path %>
