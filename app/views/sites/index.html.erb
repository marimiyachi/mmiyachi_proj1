<h1>Listing sites</h1>

<table>
  <tr>
    <th>Site</th>
    <th>Count</th>
    <th></th>
  </tr>

<% @sites.each do |site| %>
  <tr>
    <td><%= site.id %></td>
    <td><%= site.count %></td>
    <td><%= link_to 'Show', site %></td>
  </tr>
<% end %>
</table>
