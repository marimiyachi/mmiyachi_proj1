<tr>
    <td><%= page.id %></td>
    <td><%= page.count %></td>
    <td><%= page.avg_time %></td>
    <td><%= page.geo %> </td>
    <td><%= link_to "Show More", page %></td>
    <td><%= link_to "Destroy", page, method: :delete, data: {confirm: 'You sure?' }%></td>
</tr>
      
